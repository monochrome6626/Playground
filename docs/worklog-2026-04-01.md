# Worklog - 2026-04-01

## Scope

Initial setup and verification for the AstroShot Planner project workspace.

## Timeline (JST)

1. Connected local Git repository to GitHub remote.
2. Confirmed credential helper configuration and initial push flow.
3. Investigated push errors and resolved missing-commit / missing-repository issues.
4. Created initial monorepo scaffold:
   - `frontend` (Next.js + TypeScript)
   - `backend` (FastAPI + Python)
   - `docs`, `infra`, `worker`
5. Added initial project documentation set:
   - vision, requirements, architecture, schema, API, algorithm, setup, operations
6. Installed PostgreSQL on Windows and verified binary location.
7. Installed Node.js LTS for frontend package management.
8. Installed frontend dependencies with `npm install`.
9. Created backend virtual environment and installed Python dependencies.
10. Adjusted dependency compatibility:
    - `pyproj==3.7.2` -> `pyproj==3.7.1` for Python 3.10 compatibility.
11. Verified service startup:
    - backend health endpoint returned `200`
    - frontend root page returned `200`
12. Stopped temporary processes after verification.

## Key outcomes

- Local project structure is in place and ready for implementation work.
- Frontend and backend dependencies are installed.
- Basic startup path has been validated end-to-end.
- Documentation baseline exists in `docs/`.

## Files added/updated (high level)

- Root config and local dev files:
  - `.gitignore`
  - `.env.example`
  - `docker-compose.yml`
  - `README.md`
- Backend scaffold under `backend/`
- Frontend scaffold under `frontend/`
- Infrastructure notes under `infra/`
- Worker notes under `worker/`
- Architecture and planning docs under `docs/`

## Known notes

- Current Python runtime is `3.10.x`.
- `backend/requirements.txt` uses `pyproj==3.7.1` to match the active Python version.
- Frontend package set includes a known vulnerability warning from `next@15.1.7`; upgrade planned in a later hardening pass.

## Logging policy (from now on)

- All executed commands will be appended to this worklog.
- Each entry will include:
  - timestamp (JST)
  - command
  - short purpose
  - result summary
- File edits and key decisions will also be appended in chronological order.

## Suggested next work items

1. Add Alembic migration files and initialize PostgreSQL schema.
2. Replace fixture API responses with repository/DB-backed implementations.
3. Implement astronomy + geometry calculation services with unit tests.
4. Add search job execution flow (sync first, worker queue second).

## Command log

### 2026-04-01 22:42 JST

- Command: `git status --short`
- Purpose: Check current modified/untracked files.
- Result: `README.md` modified; project scaffold directories and config files are untracked.

- Command: `git branch --show-current`
- Purpose: Confirm active branch.
- Result: `master`.

- Command: `Get-ChildItem -Name`
- Purpose: Confirm top-level project structure.
- Result: `backend`, `docs`, `frontend`, `infra`, `worker` plus root config files.

### 2026-04-01 22:43 JST

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record current timestamp for logging.
- Result: `2026-04-01 22:43:07 +09:00`.

- Command: `Get-Content docs\worklog-2026-04-01.md -Tail 40`
- Purpose: Inspect current tail of worklog before appending.
- Result: Existing setup summary and logging policy confirmed.

### 2026-04-01 22:44-22:54 JST

- Command: `git status --short --branch`
- Purpose: Confirm branch and pending changes before batch implementation.
- Result: `master` with scaffold files pending.

- Command: `rg --files backend`
- Purpose: Fast file listing for backend.
- Result: Failed (`rg.exe` access denied in this environment).

- Command: `rg --files docs`
- Purpose: Fast file listing for docs.
- Result: Failed (`rg.exe` access denied in this environment).

- Command: `Get-ChildItem backend -Recurse -File | Select-Object FullName`
- Purpose: Fallback backend file listing.
- Result: Succeeded; very large output including `.venv` paths.

- Command: `Get-ChildItem docs -Recurse -File | Select-Object FullName`
- Purpose: Confirm docs files.
- Result: Succeeded.

- Command: `Get-Content backend\app\core\config.py`
- Purpose: Inspect settings implementation.
- Result: Reviewed baseline config.

- Command: `Get-Content backend\app\core\db.py`
- Purpose: Inspect DB session setup.
- Result: Reviewed baseline DB setup.

- Command: `Get-Content backend\app\api\routes\spots.py`
- Purpose: Inspect fixture-based spots route.
- Result: Confirmed fixture response implementation.

- Command: `Get-Content backend\app\api\routes\landmarks.py`
- Purpose: Inspect fixture-based landmarks route.
- Result: Confirmed fixture response implementation.

- Command: `Get-Content backend\app\models\spot.py`
- Purpose: Inspect ORM spot model.
- Result: Reviewed schema alignment.

- Command: `Get-Content backend\app\models\landmark.py`
- Purpose: Inspect ORM landmark model.
- Result: Reviewed schema alignment.

- Command: `Get-Content backend\app\models\search_request.py`
- Purpose: Inspect ORM search request model.
- Result: Reviewed schema alignment.

- Command: `Get-Content backend\app\models\match.py`
- Purpose: Inspect ORM match model.
- Result: Reviewed schema alignment.

- Command: `Get-Content backend\app\schemas\spot.py`
- Purpose: Inspect spot schema.
- Result: Reviewed response model configuration.

- Command: `Get-Content backend\app\main.py`
- Purpose: Inspect API app bootstrap.
- Result: Reviewed router registration.

- Command: `git add .`
- Purpose: Stage baseline scaffold for first commit.
- Result: Failed due `.git/index.lock` permission.

- Command: `git add .` (escalated)
- Purpose: Retry staging with required permission.
- Result: Succeeded (line-ending warnings only).

- Command: `git commit -m "chore: scaffold astroshot planner baseline"` (escalated)
- Purpose: Create baseline commit before DB implementation work.
- Result: Succeeded (`master` ahead by one commit).

- Command: `git status --short --branch`
- Purpose: Confirm clean state before next phase.
- Result: Clean working tree, ahead 1 commit.

- Command: `& '.\.venv\Scripts\alembic.exe' init alembic`
- Purpose: Initialize Alembic.
- Result: Timeout in first attempt.

- Command: `& '.\.venv\Scripts\alembic.exe' init alembic` (retry)
- Purpose: Retry Alembic initialization.
- Result: Failed (`Access is denied` / untrusted executable behavior).

- Command: `& '.\.venv\Scripts\python.exe' -m alembic init alembic`
- Purpose: Safe workaround by invoking Alembic via Python module.
- Result: Succeeded; generated `alembic.ini` and `alembic/`.

- Command: `Get-Content backend\alembic.ini`
- Purpose: Review Alembic config defaults.
- Result: Confirmed placeholder DB URL.

- Command: `Get-Content backend\alembic\env.py`
- Purpose: Review Alembic environment file.
- Result: Confirmed default `target_metadata=None`.

- Command: `Get-Content backend\app\models\__init__.py`
- Purpose: Verify model import registration.
- Result: Initially empty; later updated in code edits.

- Command: `& '.\.venv\Scripts\python.exe' -m alembic revision -m "create core tables"`
- Purpose: Create initial migration revision file.
- Result: Succeeded (`71af1a0b3e83_create_core_tables.py`).

- Command: `Get-Content backend\alembic\versions\71af1a0b3e83_create_core_tables.py`
- Purpose: Inspect generated empty migration.
- Result: Reviewed and then implemented create/drop table operations.

- Command: `Get-Content docs\setup.md`
- Purpose: Update setup docs to include migration and seeding.
- Result: Baseline content reviewed and then edited.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; & '.\.venv\Scripts\python.exe' -m alembic upgrade head`
- Purpose: Validate migration apply path.
- Result: Succeeded on SQLite.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; & '.\.venv\Scripts\python.exe' -m app.scripts.seed_landmarks`
- Purpose: Validate landmark seed script.
- Result: Succeeded (`Landmarks seeded.`).

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; & '.\.venv\Scripts\python.exe' -m pytest -q`
- Purpose: Run backend tests including new DB-backed API tests.
- Result: Succeeded (`3 passed`).

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; Start-Process ... uvicorn ... --port 8001`
- Purpose: Launch temporary API server for manual endpoint checks.
- Result: Succeeded; process started.

- Command: `Invoke-WebRequest http://127.0.0.1:8001/api/landmarks`
- Purpose: Confirm seeded landmarks returned via HTTP.
- Result: Returned Tokyo Skytree and Tokyo Tower records.

- Command: `Invoke-WebRequest http://127.0.0.1:8001/health`
- Purpose: Confirm health endpoint.
- Result: Returned `{"status":"ok"}`.

- Command: `Invoke-RestMethod POST http://127.0.0.1:8001/api/spots`
- Purpose: Confirm DB-backed spot creation API.
- Result: Succeeded; returned newly generated `spot-*` id.

- Command: `Invoke-RestMethod GET http://127.0.0.1:8001/api/spots`
- Purpose: Confirm created spot retrieval.
- Result: Returned created spot in list response.

- Command: `Stop-Process -Id 62780 -Force`
- Purpose: Stop temporary API server.
- Result: Succeeded.

- Command: `Invoke-WebRequest http://127.0.0.1:8001/health` (guard check)
- Purpose: Confirm server shutdown.
- Result: `stopped`.

- Command: `git status --short`
- Purpose: Inspect changed files after implementation.
- Result: Confirmed expected backend/docs updates and new Alembic/repository files.

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Capture timestamp for log continuation.
- Result: `2026-04-01 22:53:48 +09:00`.

- Command: `Remove-Item -LiteralPath backend\e2e.db`
- Purpose: Remove temporary verification DB artifact.
- Result: Succeeded.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; & '.\.venv\Scripts\python.exe' -m pytest -q`
- Purpose: Re-run tests after cleanup and final edits.
- Result: Succeeded (`3 passed`).

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Capture final timestamp of this batch.
- Result: `2026-04-01 22:54:25 +09:00`.

## Decisions and notes from this batch

- Kept IDs as string keys and generated spot IDs with UUID fragments to avoid scope creep.
- Implemented Alembic initial migration manually instead of autogenerate to avoid DB dependency at generation time.
- Switched Alembic execution from `alembic.exe` to `python -m alembic` due executable trust restrictions.
- Added SQLite-backed API tests for fast repeatable verification in local environments.

### 2026-04-01 22:55 JST

- Command: `git add .` (escalated)
- Purpose: Stage Alembic, DB-backed route, test, and docs updates.
- Result: Succeeded.

- Command: `git commit -m "feat: add alembic and db-backed spots/landmarks"` (escalated)
- Purpose: Persist full implementation batch in one commit.
- Result: Succeeded (`04f4c04`).

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record timestamp after commit.
- Result: `2026-04-01 22:55:59 +09:00`.

### 2026-04-01 22:56 JST

- Command: `git add docs/worklog-2026-04-01.md` (escalated)
- Purpose: Stage the newly appended command log entries.
- Result: Succeeded.

- Command: `git commit -m "docs: append latest command log entries"` (escalated)
- Purpose: Persist worklog updates as a dedicated commit.
- Result: Succeeded (`b662d8d`).

- Command: `git status --short --branch`
- Purpose: Confirm repository status after latest commits.
- Result: `master` ahead of `origin/master` by 3 commits.

- Command: `git log --oneline -3`
- Purpose: Verify latest commit stack.
- Result: `b662d8d`, `04f4c04`, `5211a8e`.

### 2026-04-01 22:57-23:12 JST (Search/Match DB implementation batch)

- Command: `Get-Content backend\app\api\routes\searches.py`
- Purpose: Inspect fixture-based searches route before replacement.
- Result: Confirmed static response implementation.

- Command: `Get-Content backend\app\api\routes\matches.py`
- Purpose: Inspect fixture-based matches route before replacement.
- Result: Confirmed static response implementation.

- Command: `Get-Content backend\app\schemas\search.py`
- Purpose: Inspect search schema compatibility.
- Result: Reviewed and updated for ORM conversion.

- Command: `Get-Content backend\app\schemas\match.py`
- Purpose: Inspect match schema for detail fields.
- Result: Reviewed and updated for ORM conversion.

- Command: `Get-Content frontend\src\lib\api.ts`
- Purpose: Inspect API client defaults before real search wiring.
- Result: Reviewed; added `createSearch`.

- Command: `Get-Content frontend\src\app\search\page.tsx`
- Purpose: Inspect current search page data flow.
- Result: Reviewed; retained server fetch and replaced form behavior.

- Command: `Get-Content frontend\src\app\matches\page.tsx`
- Purpose: Inspect current matches page static-ID flow.
- Result: Reworked to query-param-driven live fetch.

- Command: `Get-Content frontend\src\lib\types.ts`
- Purpose: Inspect type definitions for payload updates.
- Result: Added `SearchRequestCreate`.

- Command: `Get-Content frontend\src\components\search-form.tsx`
- Purpose: Inspect current scaffold search UI.
- Result: Replaced with client-side executable form.

- Command: `Get-Content frontend\src\components\match-table.tsx`
- Purpose: Inspect result list rendering.
- Result: Added empty-state messaging.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; .\.venv\Scripts\python.exe -m pytest -q`
- Purpose: Validate backend after route/service replacement.
- Result: Initially failed due test isolation issues.

- Command: `$env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm run build`
- Purpose: Validate frontend after real search UI wiring.
- Result: First failed with `spawn EPERM` in sandbox.

- Command: `$env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm run build` (escalated)
- Purpose: Retry build outside sandbox.
- Result: Failed on `matches/page.tsx` `searchParams` type mismatch.

- Command: `$env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm run build` (escalated, after fix)
- Purpose: Confirm frontend type/build correctness.
- Result: Succeeded (`next build` completed).

- Command: `git status --short`
- Purpose: Inspect modified files before final verification.
- Result: Confirmed expected backend/frontend/test/docs diffs.

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record in-batch timestamp checkpoint.
- Result: `2026-04-01 23:10:32 +09:00`.

- Command: `if (Test-Path backend\e2e.db) { Remove-Item -LiteralPath backend\e2e.db }`
- Purpose: Reset temporary SQLite verification DB.
- Result: Succeeded.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; .\.venv\Scripts\python.exe -m alembic upgrade head`
- Purpose: Recreate schema for manual API verification.
- Result: Succeeded.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; .\.venv\Scripts\python.exe -m app.scripts.seed_landmarks`
- Purpose: Seed default landmarks.
- Result: First attempt failed due race when run in parallel with migration.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; .\.venv\Scripts\python.exe -m alembic upgrade head`
- Purpose: Ensure migration is fully applied before seeding.
- Result: Succeeded.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; .\.venv\Scripts\python.exe -m app.scripts.seed_landmarks`
- Purpose: Retry seed with proper ordering.
- Result: Succeeded (`Landmarks seeded.`).

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; Start-Process ... uvicorn ... --port 8002`
- Purpose: Launch temporary backend for manual endpoint checks.
- Result: Succeeded (PID `21604`).

- Command: `Invoke-WebRequest http://127.0.0.1:8002/health`
- Purpose: Verify backend is reachable.
- Result: `{"status":"ok"}`.

- Command: `Invoke-RestMethod POST http://127.0.0.1:8002/api/spots`
- Purpose: Verify spot creation on DB-backed endpoint.
- Result: Succeeded with generated `spot-*` id.

- Command: `Invoke-RestMethod POST http://127.0.0.1:8002/api/searches`
- Purpose: Verify synchronous search execution endpoint.
- Result: Succeeded with `status: completed`.

- Command: `Invoke-RestMethod GET http://127.0.0.1:8002/api/matches?search_request_id=...`
- Purpose: Verify matches retrieval for created search.
- Result: Succeeded; `total: 0` for tested criteria.

- Command: `Invoke-RestMethod GET http://127.0.0.1:8002/api/searches/{id}`
- Purpose: Verify search retrieval endpoint.
- Result: Succeeded; request persisted.

- Command: `Stop-Process -Id 21604 -Force`
- Purpose: Stop temporary verification server.
- Result: Succeeded.

- Command: `Invoke-WebRequest http://127.0.0.1:8002/health` (guard check)
- Purpose: Confirm server shutdown.
- Result: `stopped`.

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record end timestamp of this batch.
- Result: `2026-04-01 23:12:42 +09:00`.

## Decisions and notes from search/match batch

- Implemented search execution synchronously in `POST /api/searches` to unblock end-to-end flow quickly.
- Added real astronomy + geometry calculation services and tolerance-based matching.
- Kept zero-result behavior valid and explicit (`total: 0`) for strict criteria.
- Added API-level tests for `spot -> search -> matches` flow.
- Confirmed frontend build compatibility on Next.js 15 after `searchParams` type adjustment.

### 2026-04-01 23:13-23:14 JST (final validation pass)

- Command: `git status --short --branch`
- Purpose: Inspect modified files and branch state before final validation.
- Result: `master` ahead by 4 commits with expected modified/new files.

- Command: `git diff --name-only`
- Purpose: Enumerate changed files in this batch.
- Result: Returned backend/frontend/docs change list.

- Command: `$env:DATABASE_URL='sqlite:///./e2e.db'; .\.venv\Scripts\python.exe -m pytest -q`
- Purpose: Re-run backend tests after additional route validation checks.
- Result: Succeeded (`4 passed`).

- Command: `$env:Path='C:\Program Files\nodejs;'+$env:Path; npm run build` (escalated)
- Purpose: Re-run frontend build after final backend/frontend adjustments.
- Result: Succeeded (`next build` completed).

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record final timestamp for this implementation pass.
- Result: `2026-04-01 23:14:41 +09:00`.

### 2026-04-01 23:15 JST

- Command: `git status --short`
- Purpose: Confirm final file set before staging the implementation batch.
- Result: Expected backend/frontend/docs modifications and new repository/service/test files.

- Command: `git add .` (escalated)
- Purpose: Stage full `searches/matches` DB implementation and UI wiring changes.
- Result: Succeeded.

- Command: `git commit -m "feat: implement db-backed search and live match flow"` (escalated)
- Purpose: Persist implementation in a single feature commit.
- Result: Succeeded (`4406372`).

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record timestamp after feature commit.
- Result: `2026-04-01 23:15:44 +09:00`.

### 2026-04-01 23:16 JST

- Command: `git status --short --branch`
- Purpose: Verify repository cleanliness and ahead/behind state after commits.
- Result: `master` ahead of `origin/master` by 6 commits, working tree clean.

- Command: `git log --oneline -6`
- Purpose: Verify latest commit chain after implementation and worklog updates.
- Result: Latest commits confirmed: `3cb449a`, `4406372`, `aa2d2f7`, `b662d8d`, `04f4c04`, `5211a8e`.

### 2026-04-01 23:18 JST

- Command: `git status --short --branch`
- Purpose: Check current branch/divergence and uncommitted files before planning next work.
- Result: `master` ahead by 6 commits, with `docs/worklog-2026-04-01.md` modified.

- Command: `git log --oneline -8`
- Purpose: Confirm latest commit stack for current status report.
- Result: Latest commits include `3cb449a`, `4406372`, `aa2d2f7`, `b662d8d`, `04f4c04`, `5211a8e`, `ff0c3bd`.

- Command: `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`
- Purpose: Record timestamp for this status check.
- Result: `2026-04-01 23:18:18 +09:00`.
