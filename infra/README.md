# Infrastructure Notes

- Local PostgreSQL data is stored under `infra/postgres-data`.
- Future database migrations can live under `infra/migrations`.
- The root `docker-compose.yml` is intended for local development only.
